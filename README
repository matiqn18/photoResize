# Kwadratowy obraz z przezroczystym tłem

Ten skrypt konwertuje obrazy z folderu `input/` na **kwadratowe PNG z przezroczystym tłem**, wyśrodkowując oryginalny obraz.

## Wymagania

- Python 3.x
- Pillow (biblioteka do obróbki obrazów)

## Instalacja

```bash
pip install pillow
```

## Użycie

1. Umieść swoje obrazy `(.jpg, .png)` w folderze `input/.`
2. Uruchom skrypt:
```bash
python photoResize.py
```
3. Gotowe obrazy znajdziesz w folderze `output/.`


Każdy obraz zostanie przeskalowany i osadzony w środku przezroczystego kwadratu o domyślnym rozmiarze 1080x1080 pikseli.

